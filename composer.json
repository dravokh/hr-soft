{
  "name": "hr-soft/api",
  "description": "PHP backend for HR Soft demo",
  "type": "project",
  "require": {
    "php": "^8.1",
    "vlucas/phpdotenv": "^5.6",
    "robmorgan/phinx": "^0.16"
  },
  "autoload": {
    "psr-4": {
      "App\\": "api/src/"
    }
  },
  "scripts": {
    "migrate": "vendor/bin/phinx migrate",
    "seed": [
      "@php -r \"if (getenv('APP_ENV') !== 'local' && getenv('ALLOW_SEED') !== 'true') { fwrite(STDERR, 'Seed execution skipped. Set ALLOW_SEED=true to run in non-local environments.' . PHP_EOL); exit(0); }\"",
      "vendor/bin/phinx seed:run"
    ],
    "serve": [
      "@php -S localhost:8080 -t api/public"
    ]
  },
  "config": {
    "sort-packages": true
  }
}
